# Production environment settings
# This file should be committed to version control with sensitive values replaced with placeholders

# Disable debug logs in production
NODE_ENV=production

# Base URL for the application
NEXT_PUBLIC_BASE_URL=https://aashishtangnami.com

# Database connection string (use placeholder, set actual value in deployment environment)
DATABASE_URL=postgresql://user:password@localhost:5432/portfolio

# JWT Secret (use placeholder, set actual value in deployment environment)
JWT_SECRET=your-production-jwt-secret-here

# Disable Prisma query logging in production
PRISMA_LOG_QUERIES=false
